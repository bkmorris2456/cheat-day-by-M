import {
    AppBar,
    Box,
    Toolbar,
    Typography,
    Button,
    IconButton,
    MenuIcon
} from '@mui/material';

export default function Navbar() {
    
    return (
        <Box sx={{flexGrow: 1 }}>
            <AppBar position="static">
                <Toolbar>
                    
                </Toolbar>
            </AppBar>
        </Box>
    )

}